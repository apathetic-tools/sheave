version: 2
project_name: sheave

builds:
  - env:
      - CGO_ENABLED=0
    main: ./cmd/sheave
    goos:
      - linux
      - windows
      - darwin
    goarch:
      - amd64
      - arm64

release:

  name_template: '{{.Tag}}'
