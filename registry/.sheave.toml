active_providers = ["claude", "cursor", "gemini", "openai"]

[providers.claude]
deployment_method = "flat-combine"
target_dir = ".claude"
filename = "CLAUDE.md"

[providers.cursor]
deployment_method = "folder-split-command-rules"
target_dir = ".cursor"

[providers.gemini]
deployment_method = "memory"
target_dir = ".gemini"
filename = "GEMINI.md"

[providers.openai]
deployment_method = "memory"
target_dir = ".openai"
filename = "OPENAI.md"
